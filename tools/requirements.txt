# Dependencias del andamiaje de prototipado (tools/). NO van en la app.
# Se instalan en un venv:  cd tools && python3 -m venv .venv
#   && source .venv/bin/activate && pip install -r requirements.txt
#
# Licencias (todas permisivas, compatibles con GPL-3.0-only del proyecto):
#   numpy        BSD-3-Clause
#   sounddevice  MIT        (bindings de PortAudio; PortAudio es MIT)
#   matplotlib   PSF-based  (solo para render de notacion XFN, no para audio)

numpy>=1.24
sounddevice>=0.4.6
matplotlib>=3.7
