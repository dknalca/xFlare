# Estado de los modulos

Estados: `TODO` (no existe) · `WIP` (en desarrollo) · `SEALED` (terminado y
congelado, ver `ARCHITECTURE.md` seccion 6) · `BROKEN` (regresion detectada).

> Total de la suite: **766 tests en verde** (`swift test`, 2026-09-05).

| Modulo | Capa | Estado | Sellado el | Tests | Notas |
|---|---|---|---|---|---|
| `CXFAudioCore` | 0 | WIP | — | 84 verdes | `xf_ring` (B4.1) + `xf_player` resampling antialiasing + **bucle** + **puerta por velocidad** (el plato parado no suena ni zumba) (B4.3) + `xf_metronome` (B4.4, ADR-007) + `xf_engine` motor RT + host CoreAudio (B4.2: nucleo `xf_engine_render` testeable; host `xf_engine_start`/`xf_engine_start_output` -solo salida-, sin tests) + **base instrumental como 2o player, tempo-lock por resample** (ADR-039) + getters `xf_engine_scratch_playhead`/`_seek_scratch` + `xf_rt`. El ancla de posicion del scratch mueve por velocidad media de bloque, no a saltos de cabezal (ADR-042: quita el crujido al scratchear). Corregido un SIGILL latente en `xf_engine_stop` (`os_workgroup_leave` desde el hilo equivocado). **Optimizacion del hilo RT (2026-09-03):** `xf_player_render` (el bucle mas caliente) ahora acumula la convolucion de 32 taps en `float` (antes `double`), con camino rapido contiguo sin ramas cuando la ventana cae entera dentro del sample, y **se salta los 32 taps solo con el plato EXACTAMENTE parado** (`av == 0`, tras flush a cero de la velocidad denormal — el one-pole nunca llega a 0 y una velocidad denormal dispara el modo denormal de la FPU, ~100x más lento en Intel, que al scratchear despacio se notaba como tirones; un umbral con banda también cortaba el audio al cruzar velocidad ~0). `floor()` -> truncado (el cabezal siempre es >= 0). `xf_engine_set_instrumental_native_bpm` reajusta el ratio de la base **sin recrear el player** (TAP tempo / editar el BPM en caliente). Comportamiento igual dentro de tolerancia. **EQ de 3 bandas del sample (2026-09-03, ADR-061):** `xf_eq` — low-shelf 200 Hz / peaking 1 kHz / high-shelf 4 kHz (RBJ cookbook), biquads TDF-II, SOLO sobre el reproductor de scratch (base y metronomo intactos). `xf_engine_set_sample_eq(low_db, mid_db, high_db)` desde el hilo normal disena los coeficientes (doble buffer torn-free) y el RT hace una rampa de ~20 ms hacia ellos (sin click); en plano (0/0/0) el filtrado se salta entero. **`xf_engine_set_scratch_glide_ms` / `_speed_gate`** afinables desde Ajustes › Debug (glide bajado de 5 a 3 ms por defecto: el audio sigue mejor al gesto). **Metrónomo pegado a la rejilla dibujada:** `xf_engine_set_metronome_offset(ticks)` (desfase discreto de ◀/▶, se re-fasea sin clic) **+** `xf_engine_set_metronome_drift(ticks)` (corrección **continua** de la deriva entre el reloj del motor -cristal de audio- y el de la sesión -timer de pared-; la capa Swift la empuja suavizada 20×/s = `session.tick()+gridShift - engine.tick`, sin re-fasear). Un `seek_tick` pone ambos a 0. **Pasada de optimización (2026-09-04, ADR-065):** la vuelta del bucle de `xf_player_render` (base instrumental) cambia `fmod` por un par de sumas/restas — `|v|` es <<< `frames`, resultado idéntico, una llamada a libm menos por muestra. **Bucle de una REGIÓN de la base (2026-09-04, ADR-067):** `xf_player` gana `loop_start`/`loop_end` (frames); con loop activo el cabezal y la lectura sinc envuelven dentro de `[loop_start, loop_end)` — bucle de una parte tan continuo como el del fichero entero. `xf_player_set_loop_region` (la región se lee 1×/bloque y se SANEA en el render: una lectura rasgada nunca sale de `[0,frames)`). `xf_engine_set_instrumental_loop_region` + `xf_engine_seek_instrumental`. Por defecto = sample entero (sin cambio). 74 tests en verde.  **Más cubos de ratio de resampling (2026-09-04, F.45, ADR-072 iter.):** `XF_PLAYER_RATIOS` pasa de 7 cubos a ojo (techo 8x) a 24 log-espaciados de 1x a 16x — transición de brillo pareja al barrer la velocidad, sin escalones, y un scribble/crab rápido ya no se queda pegado al kernel de 8x. Coste RT igual (misma búsqueda acotada); la tabla (~1,5 MB) se calcula una vez al crear el player. **Rampa de velocidad dentro del bloque (2026-09-04, F.46, ADR-072 iter.):** `xf_player_render` deja de tomar una velocidad OBJETIVO constante por bloque y pasa a `(target_velocity_start, target_velocity_end)`, interpolados linealmente muestra a muestra; `xf_engine` guarda `prev_target_velocity` (solo hilo RT) y arma la rampa `prev_target_velocity -> target_velocity` en cada bloque del reproductor de scratch (la base instrumental sigue con velocidad constante, start==end: su ratio de tempo-lock no scratchea). Con buffers grandes ya no hay un escalón que perseguir en cada frontera de bloque. 77 tests en verde. **Puerta con taper suave + bloqueador de DC (2026-09-04, F.47, ADR-072 iter.):** la puerta por velocidad pasa de rampa lineal (esquina dura en `av=gate`, se notaba en cada inversión de sentido) a taper de COSENO ALZADO (pendiente cero en los dos extremos). Dentro de la zona de puerta (`av < gate`, no fuera) un bloqueador de DC de un polo (corte ~38 Hz) quita el zumbido del cabezal casi quieto; el umbral por defecto baja de 0,12 a 0,04. **Regresión atrapada por los tests antes de commitear:** la primera versión aplicaba el bloqueador SIEMPRE que la puerta estaba configurada (también a velocidad normal), borrando el grave/casi-DC de cualquier sample; `XFEngineRTTests.testSoftClipYPicoDeSalida` lo delató. Arreglado: el bloqueador solo actúa dentro de la zona de puerta. 80 tests en verde. Falta la puerta de latencia en hardware + Instruments (B4.5) para sellar (B4.6). **Selector de canal al arrancar (2026-09-04, F.60):** `xf_engine_start`/`xf_engine_start_output` ganan `input_channel`/`output_channel` (1-based) + `kAudioOutputUnitProperty_ChannelMap` al arrancar (no en el callback RT). Motivado por B5.5/B1.2: en la Rane 72 (14 in/10 out) el canal 1 casi nunca era el que hacia falta. **Salida separada scratch/instrumental (2026-09-05, F.68, ADR-075):** `xf_engine_render` gana `out_instr_l`/`out_instr_r` — el modo lo decide la propia llamada (los CUATRO punteros no-nulos = dos buses independientes con su propio soft-clip; falta cualquiera de los dos de la base = combinado, bit a bit igual que antes). `xf_engine_start`/`xf_engine_start_output` ganan `instrumental_channel` (`<=0` o igual al del scratch = combinado, ASBD 2 canales; distinto = separado, ASBD 4 canales + `ChannelMap` de dos pares). La captura de entrada se queda siempre en 2 canales. No verificado por hardware con los oídos todavía. 84 tests en verde. **Silencio (no clamp) antes del principio del sample (2026-09-05, F.70, ADR-076):** `xf_player_render`, sin bucle, ya NO satura el cabezal por abajo (solo por arriba, fin del sample) — con timecode real el vinilo puede girar hacia atrás más allá del principio y el cabezal debe seguir integrando ese giro, no clavarse en 0 (si no, al volver hacia delante el sample sonaba antes de tiempo: se perdía la referencia con el vinilo real). La convolución YA devolvía silencio fuera de `[0, last]` (rama de los bordes); solo hacía falta dejar el cabezal negativo, lo que rompía el atajo "truncar == floor" (`floor()` de verdad solo cuando el cabezal es negativo; camino rápido con `x >= 0` intacto). 86 tests en verde. |
| `XFPrimitives` | 0 | SEALED | 2026-09-01 | 4 verdes | apiVersion 1. `MotionSample` / `FaderSample` (value types compartidos capture↔analysis). ADR-033 |
| `CXFTimecode` | 0 | SEALED | 2026-09-04 | 7 verdes | xwax 1.10 vendorizado intacto (B5.1) + wrapper `xf_timecoder` en modo relativo (B5.2), hamster/reverse (B5.3), confianza + dropout (B5.4). Tests con señal de cuadratura sintética. **B5.5 (2026-09-04):** validado con vinilo Serato CV02 real sobre la Rane 72 (`spike/b5-timecode/tcprobe`): 60s a 33⅓ → vel media 0.9999, conf 0.92-1.00, engancho bitstream SI; dirección (scratch invierte signo/dir) y dropout (conf decae a 0 sin colgarse) confirmados. El canal correcto no era el que asumía el perfil ni el que la propia mesa etiqueta "Deck 1" — fue "Analog 1" (1-2); detalle en `docs/TIMECODE.md` §3 |
| `XFClock` | 1 | SEALED | 2026-08-31 | 34 verdes | apiVersion 1. Tick/PPQ 480, Tempo, TimeSignature, HostClock, ClockMap, Transport. ADR-031 |
| `XFNotation` | 1 | SEALED | 2026-09-01 | 20 verdes | apiVersion 1. Modelo XFN + compose (port de xfn_core.py) + crop con tramo parcial + variantes offset/amplitude/mirror/swing/subdivision + ScoreEvents (== SCORING.md) + golden vs library-v0.1.json. ADR-032 |
| `XFProfiles` | 1 | SEALED | 2026-08-31 | 24 verdes | apiVersion 1. Parser INI propio (ADR-019), extends + herencia circular, validacion == xf_profile.py, autodeteccion con comodines, precedencia bundle/usuario |
| `XFCapture` | 1 | SEALED | 2026-09-05 | 90 verdes | Protocolos (B6.1), teclado (B6.2), `TimecodeMotionSource` (B6.3, **validado con vinilo real en B5.5**), `FaderBinarizer` (B6.5), `.xfsession` + `Replay*Source` (B6.6), `PracticeCommandMidi` (ADR-054, comandos de práctica + MIDI Learn). Crossfader (ADR-021): **MIDI** (`MidiFaderSource`/`MidiCrossfaderConfig`) es el método primario, **confirmado con la Rane 72 real** (CC8/canal16, 15313/15317 mensajes limpios) a través de `MidiMonitorConnector` (CoreMIDI genérico, un cliente para toda la sesión, F.61) — el `MidiFaderConnector` dedicado que se planeó al principio nunca llegó a instanciarse en la app y se borró como código muerto (2026-09-05). `MidiFaderLearner` (F.67, puro) descubre qué `(canal, cc, rango)` es el crossfader moviéndolo de tope a tope, sin depender de que el perfil lo declare. `audio_return` (`AudioReturnFaderSource`, tono piloto) y HID (`HIDFaderConnector`) quedan como **respaldos sin confirmar con hardware real** — no bloquean el sellado, el método primario sí está validado extremo a extremo. Ver `Sources/XFCapture/README.md`. |
| `XFAnalysis` | 1 | WIP | — | 19 verdes | Emparejado de clicks, DTW de contorno, sigma/sesgo, amplitud, scoring por evento (== SCORING.md), 3 estrellas (ADR-025), diagnostico NL (ADR-018). `MotionResampler.velocity` ya no revienta si el patron dura mas que la toma (underflow `UInt64` -> SIGILL); devuelve el extremo, como `position`. `Diagnoser` **siempre devuelve >=1 frase** (antes una toma limpia de un patron sin clicks -baby- daba lista vacia). B8.5 sintetico completo: `ReplayScoringTests` fija las invariantes good/late/sloppy contra un patron de cada nivel 1-4 (forward-cut / transformer-2 / flare-1c / flare-2c); `GoldenReplayScoringTests` congela el `Report` numerico exacto de las 12 tomas en `Fixtures/golden/analysis/` (redondeo a 4 decimales + tol 1e-9, `make golden-update`). Sin sellar: B8.5 necesita `.xfsession` reales (hardware), B8.4 umbrales por afinar contra ellos |
| `XFPersistence` | 1 | SEALED | 2026-09-01 | 44 verdes | apiVersion 1. `XFDatabase` (puerta unica, GRDB 6.x, migracion `v1` con 10 tablas). Records + metodos por concern: histrico de tomas + eventScores (B10.2/B10.6), progreso agregado (B10.7, == SCORING.md §3), dominado + desbloqueo de variantes (B10.8), repeticion espaciada 1/3/7/21 (B10.3), calibracion por dispositivo (B10.4). Reglas de producto dentro, catalogo fuera. ADR-035 |
| `XFEngine` | 1 | SEALED | 2026-09-01 | 38 verdes | apiVersion 1. Facade `Session` (B9.4, ADR-034) cableando `SessionMachine` (fases), `BPMLadder` (tempo adaptativo: 3 aprobados suben, 2 fallos bajan) y `UnlockRule`/`UnlockTracker` (desbloqueo por N compases buenos seguidos, no por media). Todo struct valor que avanza por eventos como `Transport`. Una serie se aprueba por streak de compases limpios, no por media (ADR-034) |
| `XFDesign` | 2 | SEALED | 2026-09-01 | 7 verdes | apiVersion 1. Tokens (color, espaciado, tipografía, HitLevel) + componentes base (XFCard, XFButtonStyle, BPMStepper, HitBadge). macOS 11. ADR-036 |
| `XFRender` | 2 | SEALED | 2026-09-01 | 49 verdes | apiVersion 1. `HighwayLayout` (autopista: fantasma + capa de usuario teñida por `HitLevel` + cabeza de lectura + **rejilla de negras/compas** + **phantom clicks**, loop con el patron, anti-deriva `frame(T)==frame(T+L)`). `ScopeLayout` (scope circular, Lissajous reconstruido de `position`/`confidence`). `HighwaySVG` (golden SVG de los 25, con rejilla). Escenas/vistas SpriteKit+SwiftUI delgadas, sincronizadas al reloj de AUDIO. **B7.2b cerrado (2026-09-03):** 60 fps estables medidos en el MBP 2015 Intel (overlay `FrameRateMeter` + `ScreenRefresh` en XFApp, sin tocar este módulo). ADR-036, ADR-038, ADR-040, ADR-041, ADR-044 |
| `XFTestKit` | — | UTIL | — | 18 verdes | Utilidades **solo para tests** (no entra en el binario; en el grafo para que los `*Tests` lo importen). Depende de XFCapture / XFNotation / XFPrimitives. **`Golden`** — comparación de goldens tolerante a la arquitectura (ADR-028): `round4` + `approxEqual` / `firstMismatch` con tol `1e-9` (ya lo usaban XFNotation/XFRender/XFAnalysis; ahora con tests propios). **`Signals`** — señales sintéticas deterministas: `sine`, `silence` (PCM mono float), `quadratureTimecode` (vinilo de timecode falso, estéreo int16; `carrierHz` = velocidad, `secondaryPhaseDeg` = sentido). **`FakeMotionSource` / `FakeFaderSource`** — implementaciones de mentira de los protocolos de `XFCapture` (modo *script* o *valor fijo*, conteo de `start()`/`stop()`, `startError`); para todo lo que consume captura sin hardware ni ficheros. **`RepoFiles`** — `root()` / `url(_:)` / `data(_:)` / `text(_:)` para leer del repo (`data/`, `Fixtures/`, goldens) subiendo desde `#filePath` hasta `Package.swift`. **`Fixtures/`** recurso empaquetado (`XFTestKit.fixturesURL`). `scaffoldingVersion` sigue en 0 (smoke test de `XFEngineTests`, sellado). ADR-028 · ADR-066 |
| `XFApp` | 3 | WIP | — | 311 verdes | Ensamblaje: `CatalogLoader` (carga `data/` + `PrimitiveSet`), assemblers (Home/Library/ExerciseDetail/Variant/MyTable), `AttemptRecorder`, `EngineHandle` (envoltorio de `xf_engine`, con `restartOutput` para cambiar el buffer en caliente), `AppModel` (coordinador + navegacion; `scratch(exerciseId:variantId:)` aplica el transform de la variante, ADR-043) y `AppRootView` (sustituye `HomeScaffoldView`, ya borrada). Pinchar un truco abre su **ficha** (dibujo + historia + variantes con marca) antes de practicar; la onda de la instrumental va arriba con la rejilla alineada a la autopista. El `@main` monta `AppModel.boot()` con el loader que toque: `BundleContentLoader` (recursos del `.app`, los copia `make app`) si el bundle trae el catalogo, si no `RepoContentLoader` (repo, para `swift run`/tests). Practica rudimentaria jugable con audio (scratch por velocidad + ancla-trim ADR-042, base instrumental, medidor de clip, volumenes/sensibilidad/buffer por sesion); ajustes (buffer, nombre) persistidos en plist local, los volumenes no (eran la causa de "no suena nada"). + B11 completo (16 tareas): asistente de calibracion (B11.1), Home + matriz + racha (B11.2), practica/autopista (B11.3), resultados + diagnostico (B11.4/B11.13), modo libre 30 s (B11.5), navegador de libreria (B11.6), ajustes (B11.7), accesibilidad (B11.8), Mi mesa (B11.9), mapeo MIDI/HID (B11.10), exportar perfil (B11.11), permiso de microfono (B11.12), progreso por variante (B11.14), selector de variantes (B11.15), Rosetta (B11.16). Modelos/formatters testeables + vistas SwiftUI (macOS 11); el cableado con datos reales y la verificacion visual quedan para cuando corra la app. **+ (sep 2026)**: practica sectorizada (ADR-053), **Freestyle** (nav), grabacion de linea **anclada a la instrumental** (ticks, cuadre a compases, claqueta, `.xfsession`), **"Puntuar la toma"** -> `XFAnalysis` -> resultados, tecla `2` reinicia la base, **F.3** `SampleTrim` (punto cero) + **biblioteca de samples** (`AppSettings.sampleLibrary`, menú en el panel Mezcla) + **cue points A/B** (`PracticeSession.jumpTo(sampleFraction:)`) + aviso de loop rítmico (`TempoAnalyzer`) + **EQ Lo/Mid/Hi del sample** (`EngineHandle.setSampleEQ` → `xf_eq`, 3 sliders en el panel Mezcla, por sesión; ADR-061), **F.0** `WarmupPlanner`/`WarmupOxidation`/`WarmupView` + nav "Calentar" (+ **rutina de arranque** fija sin historial: Forward Cut → Reverse Cut → Chirp → Transformer, 8 frases de 2 compases; arranca en "repite conmigo"). El calentamiento entero corre en **una sola sesión**: `startWarmupSession(rows:)` → `[WarmupStep]`, `PracticeSession.reload(scratch:)` cambia el patrón en caliente y `LivePracticeView.advanceWarmup()` encadena al completar las frases (barra "Calentamiento i/N"). El plan es **editable** en `WarmupView`: borrar fila, ×2 / ÷2 sobre el nº de frases, "+" añade de `AppModel.warmupLibrary` (`WarmupPickable`). En "tu turno" del call & response se atenúa **solo el fantasma gris**, no la traza teal (antes se oscurecía todo). **Instrumental subida** (ADR-060): si **parece un loop** (`TempoAnalyzer.isShortLoop`, o sin tempo detectable) entra en "modo loop" — `InstrumentalLoop` (puro) deriva el BPM de N compases y la duración, suena a velocidad natural y aparecen botones **−/+** en el panel Base. Una **pista larga** con tempo claro va por la detección normal (BPM detectado + rotación de fase para alinear el "1"), como el asset — corregido tras una regresión el mismo día. El BPM de la base se puede **rematar a mano**: botón **TAP** (`TapTempo` puro) y pinchar el número lo abre para escribirlo; ambos van por `setInstrumentalBPM`, que cambia la rejilla y el metrónomo **en caliente** (sin reiniciar la base ni el reloj: `setInstrumentalNativeBPM` reajusta el ratio de la base para que siga sonando donde estaba). En modo loop lo traduce al nº de compases entero más cercano. **Metrónomo alineado con la rejilla**: "reiniciar la base" (tecla `2`), importar línea y cargar otra base hacen `engine.seek(tick: 0)` además de `session.resyncClock()`; los botones ◀/▶ de la rejilla mueven **rejilla + fantasma (dibujado y oído, vía `PracticeSession.gridPhaseTicks`) + metrónomo (`engine.setMetronomeOffset`)** como un bloque, mientras que **la traza y la tira de instrumental NO se mueven** (`PracticeScene.renderUserTrace(now: rawNow)`): así la traza bajo el cabezal es lo que suena, y el fantasma visto = el oído. Y **`retempo` (÷2/×2) ya no reinicia la base** — delega en `setInstrumentalBPM` (cambio en caliente, la base sigue sonando donde estaba). La **tira de la instrumental** se dibuja pegada al **cabezal del audio** (`xf_engine_instrumental_playhead` → `EngineHandle.instrumentalProgress` → `PracticeScene.renderStrip` usa `headFrac * loop`), no a `now mod loop`: así no se descuadra del sonido al cambiar el tempo (`instrumentalLoopTicks` cambia sin mover el reloj). El **TAP tempo** (`TapTempo`) ahora espera **4 golpes** y usa la **media recortada** (descarta el golpe que se desvía >35 % de la mediana). **Call & response**: al empezar tu turno el fader se **reabre** (el fantasma pudo dejarlo cerrado al final de la escucha y arrancabas mudo hasta tocar Espacio). **F.0 cerrado**: "Puntuar (calentamiento)" → `AppModel.scoreWarmupTake` → `settleWarmupTake` (`mode:.warmup`, no cuenta para estrellas, repetición espaciada, `setOxidized` si <2★); el resultado (estrellas · % · aviso de oxidación) se enseña en un panel dentro de la práctica, sin ir a `.results`. **F.4** `TakeVideoExporter` (mp4 con la **proporción de la ventana**, **audio** del `xf_engine` offline **con la mezcla del mixer**, barra de progreso, la línea **refleja los cortes de fader**, **fps/resolución en Ajustes**; ADR-056/057). **MIDI de comandos** (`PracticeCommandMidi` en XFCapture + `AppModel.midiCommands`/`practiceCommandEvents` + fila en Ajustes; ADR-054). **MIDI Learn** (ADR-059): `MidiLearnModel` + `MidiMonitorConnector`; en Ajustes seleccionas el comando, pulsas "Aprender MIDI" y el siguiente control que muevas queda asignado (o lo escribes a mano). **Miniaturas TTM**: la celda dibuja la curva del disco entera, tramo lleno = suena / a rayas = cortado, + recuadro "cómo leer el gráfico" en Home (ADR-055). **Ajustes** reescrito sin `Form` (macOS 11; ADR-058) y partido en **pestañas** (General / MIDI / **Debug**). La pestaña **Debug** afina el "tacto" del plato — glide, puerta de velocidad, fricción, sensibilidad del trackpad — con sliders persistidos en `AppSettings`; `LivePracticeView` los aplica al abrir la práctica (`engine.setScratchGlideMs/…`, `session.frictionPerSecond`). Pantalla de carga con cita (`citas.md`). Splash animado. `SplashView`, `XFWordmark`. **Menú reestructurado (2026-09-03):** el navegador de trucos pasa a llamarse **Trucos** (`LibraryView`, ahora en **tarjetas** con la notación TTM de cada truco a la derecha, `LazyVGrid`); nuevo menú **Librería** (`MediaLibraryView`, `screen .mediaLibrary`) con pestañas **Instrumentales** / **Samples**: añadir ficheros o **una carpeta entera** (con opción de subcarpetas), **arrastrar y soltar**, quitar. **Fase 2 — pre-análisis** (`InstrumentalAnalysisCache`, `~/Library/Application Support/xFlare/instrumental-analysis.json`): al añadir una instrumental se analiza el tempo UNA vez en segundo plano y se cachea (invalidado por tamaño/mtime del fichero y por la sample rate); en la práctica, `loadInstrumental` lee del caché y carga al instante. La tarjeta muestra "analizando…" / "≈ N BPM · M compases". Aviso `NSAlert` si se añaden ≥ 20 pistas de golpe. `TempoAnalyzer.Result: Codable`. **Fase 3 — samples a botones MIDI (2026-09-03):** la pestaña Samples de la Librería tiene una sección **SLOTS MIDI** con 4 slots (`AppSettings.sampleSlots`, siempre 4, `""` = vacío) asignables desde un `Menu`; los comandos `Sample 1`…`Sample 4` (XFCapture) mapeados a botones de la mesa hacen `cue` + cargan el slot en caliente (`LivePracticeView.loadSlot`). Y el **selector de instrumental de la práctica** es un `Menu` que lista las instrumentales analizadas de la librería (`instrumentalLibrary`, cargan al instante del caché) además de la base por defecto y "Cargar otra…". **Persistencia en fichero (2026-09-03, ADR-063):** `SettingsStore` guarda `AppSettings` como JSON legible/copiable en `~/Library/Application Support/xFlare/settings.json`, atómico en cada cambio (antes solo `UserDefaults`, que `cfprefsd` vaciaba tarde y perdía cambios si la app se cerraba de golpe); `loadSettings` prefiere el fichero, migra el plist viejo si hace falta. **Reorg de la práctica (2026-09-03, ADR-064):** columna izquierda `leftColumn` con todo lo del SAMPLE (selector, **slots MIDI** `sampleSlotsRow` que asignan y disparan los 4 slots, cue A/B, meter, EQ, volúmenes); zona inferior `bottomBar` = fila compacta (nombre + reiniciar/÷2/×2/◀/▶/TAP/BPM) que se despliega (`instrLibraryPanel`) a la lista de instrumentales analizadas de la Librería y se minimiza sola al cargar. `LivePracticeView` gana `onSampleSlotsChanged`. **BPM con un decimal:** `PracticeSession.bpm` pasa de `Int` a `Double` (`setBPM` redondea a 0,1), `TapTempo.tap()` devuelve `Double?`, `PlatterInputView.onBPM`/`currentBPM` → `Double`; la UI formatea `%.1f` y acepta coma/punto. **Tope del sample:** `AudioAsset.scratchMaxSeconds` (2 s) + `capScratch` — un fichero largo ya no barre minutos de audio con un gesto. **Números de rejilla:** `PracticeScene.gridLabels` (puro) da "compás.subdivisión" (1.1, 1.2…) sobre cada línea, arriba, discretos, siguiendo `gridShift`. **Pasada de optimización (2026-09-04, ADR-065):** `SKLabelNode.text`/`isHidden` solo se escriben si cambian (evita re-teselar el glifo cada frame); `gridLines`/`gridLabels` rellenan buffers reservados (0 `malloc`/frame); `renderUserTrace` trocea la traza sin arrays intermedios; `traceBuffer` con `reserveCapacity` y poda por `removeFirst(k)`; la telemetría a 20 Hz (pico + deriva del metrónomo) sale a `ClipMeterView` para no re-evaluar el `body` de `LivePracticeView` 20×/s. Sin cambio de comportamiento — 638 tests igual. **Editor de instrumental (2026-09-04, ADR-067):** `InstrumentalEdit` (puro, `Codable`: bpm?, downbeatSeconds?, beatsPerBar, cues, loops, activeLoopID) + `InstrumentalEditStore` (`~/Library/Application Support/xFlare/instrumental-edits.json`, por ruta) + `AppModel.instrumentalEdits`. `InstrumentalEditorView` (`Screen.instrumentalEditor(path:)`, botón de ajustes en cada fila de la Librería): reproduce de verdad (engancha `EngineHandle`: play/pausa, pinchar la onda = saltar, activar una región = oír el loop), onda + rejilla de compases con números arriba (SwiftUI, sin `Canvas`), BPM/TAP/÷2·×2/"fijar el 1 aquí"/compás, cues (añadir en el cabezal, renombrar, saltar, borrar), regiones de loop (crear 4 compases, nudge inicio/fin, activar, borrar). En la práctica, `loadInstrumental` usa el `InstrumentalEdit` si lo hay (rama "instrumental editada": BPM y "1" del editor mandan sobre la detección; la región de loop activa se traduce a coords rotadas y se aplica con `setInstrumentalLoopRegion`). Pendiente v2: re-aplicar la región tras ÷2/×2/reiniciar. **Iteración 2 (2026-09-04):** la onda del editor se **re-renderiza** al hacer zoom (solo el tramo visible, `renderWindow`, gen counter) en vez de estirar la imagen entera; **regiones de loop con ÷2/×2** (`scaleLoop`); los **Cue de la instrumental se disparan por MIDI** (`instrCue1…4` → `jumpInstrCue`: salta la base al cue y re-cuadra la rejilla) y por botones en la zona inferior; **Ajustes › MIDI por categorías** (global / sample / instrumental). **REGRESIÓN arreglada:** `SettingsView` guardaba todo `AppSettings` en un `@State` viejo que pisaba la librería de instrumentales al primer cambio — re-siembra desde la copia entrante + `AppModel.recoverInstrumentalLibraryIfNeeded` (una vez). **Editor de samples (2026-09-04, ADR-068):** `SampleEdit` (puro, `Codable`: `startSeconds` + `lengthSeconds` acotado a `AudioAsset.scratchMaxSeconds`) + `SampleEditStore` (`sample-edits.json`, por ruta) + `AppModel.sampleEdits`. `SampleEditorView` (`Screen.sampleEditor(path:)`, botón de ajustes en cada fila de la pestaña Samples): onda con zoom (`renderWindow`), ventana de recorte con dos asas arrastrables (inicio/fin, arrastrar el interior mueve toda la ventana), `◀/▶` de inicio y duración, "usar todo", y **"Escuchar el recorte"** en bucle (`EngineHandle.previewLoop` con el reproductor de la base a velocidad natural). En la práctica, `loadScratchSample` usa `pcm[SampleEdit.frameRange]` si lo hay, si no `SampleTrim`. `LivePracticeView` gana `sampleEdit`; `MediaLibraryView` gana `onEditSample`. **Asistente de calibración con dispositivos reales (2026-09-04, ADR-073):** `AudioDeviceList` (nuevo) enumera los dispositivos de audio del sistema (mismas llamadas CoreAudio que `spike/b1-latency/passthrough --list`); `AppRootView` guarda el asistente en `@StateObject` (antes se reconstruía en cada render del body, perdiendo la selección) y le pasa la lista real en el paso 1. Los pasos 2-4 (sin motor de captura, B4.2) muestran un aviso en vez de un control mudo (`CalibrationStep.pendingNote`). `MyTableView.onCalibrate` navega a la pantalla de Calibración (antes no estaba conectado). **Cues/loops en la práctica + navegación (2026-09-04, ADR-069):** `TransientDetector` (puro) marca los ataques del sample en `SampleEditorView` (marcas amarillas + "inicio al transitorio anterior/más cercano/siguiente"; el `◀/▶` de Inicio baja a 5 ms). Los **cues y la región de loop** del editor de instrumental se **ven** en la práctica sobre la tira superior (`PracticeScene` gana `instrumentalCues` [fracciones 0…1] e `instrumentalLoopRegion`; `LivePracticeView` guarda `instrDownbeatSec` para mapear coords del fichero a la tira rotada). **Navegación** por botones (fila "Cues" + nueva fila "Loops") y **MIDI** (`PracticeCommand` +5: `instrCuePrev`/`instrCueNext`, `loopJump`/`loopPrev`/`loopNext`; recorren en círculo y aplican la región con `EngineHandle.setInstrumentalLoopRegion`). Aritmética de bordes en `InstrumentalNav` (puro, testeado). **Descomposición mano / fader (F.23, ADR-071):** `PracticeSession.AssistMode` (`both`/`hand`/`fader`) — la máquina lleva la capa que sueltas, muestreada de la misma curva del patrón que la escucha; ortogonal al "repite conmigo" (en `listen` la máquina lleva las dos). Sección "Manos" en el rail derecho (también Freestyle), insignia ámbar en la barra superior, comando MIDI `assist_cycle`. `setFaderClosed` (input) se ignora si la máquina lleva el fader; rutas internas por `applyFaderClosed`. **Tacto — 1ª tanda (F.01/F.03/F.04/F.08, ADR-072):** `PlatterInputView` descarta la inercia del trackpad (`momentumPhase != []`); `LivePracticeView.pushPlatterVelocity()` empuja la velocidad al motor en el instante del evento (no en el paso de 60 Hz); `AppSettings.defaultBufferFrames` = 128 (antes 512); `PracticeSession.decayPlatterVelocity` añade rozamiento seco de Coulomb (`coulombFriction`) que para el plato en firme. **F.44 — modelo de posición (ADR-072 iter.):** el trackpad deja el modelo de impulso (`scrollBy`) por control de posición: `PracticeSession.scrub(pointsPerSecond:)` fija `velocity = Δx/Δt · scrubGain · sensibilidad` (la vista saca `Δx/Δt` de `event.timestamp` y `event.phase`), y `coastPlatter` no aplica fricción mientras `scrubbing`. Parar la mano sin levantarla para el plato en seco. La rueda de ratón sigue con `scrollBy`. **Iteración mismo día:** la región activa del `edit` se aplica al motor **en cualquier rama** de `loadInstrumental` (antes solo si el edit traía BPM/"1" → se veía la sombra pero la base no hacía el bucle) y `seekInstrumental` deja la base dentro del loop; **`loopToggle`** (on/off) en la fila "Loops" y en la fila compacta, arranca encendido si el editor dejó una región activa; al cargar otra base se **mutea** la actual y sale un cartel "Cargando N…" (`instrLoadGen` descarta cargas superadas). **Logo (ADR-070):** subidas de tono las partes casi negras de `icon/xflare.svg` (+ `icns`) y `XFWordmark` **rediseñado** como miniatura del icono (se fundían con el tema oscuro). **Rail del sample alineado con el teal:** `PracticeScene.renderRail` situaba el sprite del sample en `0…hh`, así el inicio del sample quedaba pegado al borde inferior y dejaba un hueco hasta la traza en reposo; ahora la onda del rail usa el **mismo mapeo que `traceY`** (`railY(fraction:)`, con `scratchPatternTopFraction`) → f=0 cae exactamente en el reposo del teal. El sprite va en un `sampleCrop` (la aguja no se recorta, sigue saliéndose por arriba). **F.48 (2026-09-04):** `AudioDeviceLatency` — lee `kAudioDevicePropertyLatency`/`SafetyOffset`/`BufferFrameSize`/`NominalSampleRate` por dispositivo y lado, da `totalFrames`/`totalMs`; base para F.50/F.54, sin cablear todavía **El crossfader por MIDI llega de verdad a la práctica (2026-09-04, F.61):** `AppModel.midiMonitor` (CoreMIDI real, abierto en `boot()`) reparte cada mensaje a `midiCommands` y a `crossfaderSource` (construido del perfil activo si `crossfader.method = midi`); antes nada abría CoreMIDI durante la práctica, solo "MIDI Learn" de Ajustes. El cambio de estado llega como `practiceCommandEvents.send(.faderClosed(...))`, el mismo evento que ya escuchaba `LivePracticeView`. **El scope de Calibración lee el vinilo de verdad (2026-09-04, F.62):** `AppModel.startTimecodeCapture()` para el motor y lo reabre CON entrada (`EngineHandle.preferred*`, F.60), un `Timer` a 30 Hz drena `drainInput()` hacia un `TimecodeMotionSource` (Serato 2ª ed., el validado en B5.5) y acumula `[ScopeReading]` para el `ScopeView` que ya existía en `XFRender` pintándose vacío; `AppRootView` lo cablea al entrar/salir de `.calibration`. **Paso "Latencia" declarada, no medida (2026-09-04, F.63):** a petición del autor ("no le veo sentido" el loopback por cable, poco práctico en mesas como la Rane 72), el paso 2 del asistente pasa de pedir un cable a mostrar la latencia que el driver DECLARA (`AudioDeviceLatency`, F.48) — automático, sin botón. `AudioDeviceList.resolvedOutput`/`resolvedInput` resuelven el dispositivo de Ajustes o el de sistema por defecto. `measuredLatencyMs` se renombra a `latencyMs` (el nombre viejo afirmaba una medida que ya no se hace). **El paso 1 del asistente ahora sí elige de verdad (2026-09-05, F.64):** con la Rane 72 real conectada, el autor reportó el asistente atascado en el paso 2, sin forma de elegir el par estéreo del timecode y con el scope vacío — las tres venían de que `AudioCalibrationStep` ligaba sus Pickers a una copia local de `CalibrationWizardModel` que nunca llegaba a `AppSettings` (de donde sí leen el motor y el cálculo de latencia). `AppRootView.applyCalibrationSelection()` vuelca cualquier cambio del paso 1 a `AppSettings`, añade el selector de par estéreo (`AudioDeviceList.resolvedChannel`, nuevo) y recalcula latencia + reinicia el scope al vuelo. **Sin paso de latencia (2026-09-05, F.66):** a petición directa del autor ("no le veo sentido") se quita el paso "Prueba de latencia" del asistente — `CalibrationStep` pasa a 3 casos, `CalibrationWizardModel` pierde `latencyMs`/`reportLatency`, `LatencyCalibrationStep`/`LatencyVerdict` se borran; `AudioDeviceLatency` (F.48) queda intacta para otros usos. **Vinilo real en Freestyle/práctica, cerrado (2026-09-05, F.65):** `PracticeSession.pushRealVelocity(_:sampleDurationSeconds:)` traduce `MotionSample.velocity` (1.0 = 33⅓ rpm nominal) al espacio del patrón deshaciendo la conversión de `normalizedVelocity`, para que `engine.setVelocity` reciba el ratio real intacto; reutiliza `scrubbing`/`lastScrubAt` de `scrub()` para que un dropout (aguja levantada) devuelva la fricción sintética sola. `AppModel.motionSampleEvents` (nuevo, mismo tráfico que `onTimecodeSample` pero publisher) llega a `LivePracticeView` (`captureRealTimecode`/`motionSamples`/`startRealCapture`/`stopRealCapture`), que se suscribe con `.onReceive` y empuja la velocidad real igual que `pushPlatterVelocity`. Carrera resuelta: `loadInstrumental(initial:)` ya no llama a `engine.startOutput()` sin condición — si hay un dispositivo de entrada configurado, llama a `startRealCapture()` en su lugar; un solo sitio decide el modo del motor. **Paso "Fader" con cortes reales + "Aprender MIDI del fader" (2026-09-05, F.67):** `AppModel` gana `onRawMidiMessage` (mismo patrón que `onTimecodeSample`, ve todo el tráfico MIDI sin filtrar); `AppRootView.rebuildFaderConfig()`/`handleCalibrationMidi(...)` binarizan en vivo contra `MidiFaderLearner`/`FaderBinarizer`, con el CC aprendido en la sesión con prioridad sobre el del perfil. **Arreglo de paso:** `midiMonitor.onMessage` mutaba `@Published`/enviaba a un `PassthroughSubject` desde el hilo de CoreMIDI en vez del principal desde F.61 (`MidiLearnModel`, F.59, ya lo hacía bien) — con hardware real de por medio esto es una fuente real de UB, se corrige con `DispatchQueue.main.async`. Lo aprendido no se persiste todavía en `DeviceCalibration` (XFPersistence no tiene esos campos) ni alimenta la práctica real — pendiente, cruza de módulo. **Salida separada de scratch/instrumental en Ajustes (2026-09-05, F.68, ADR-075):** `EngineHandle.preferredInstrumentalOutputChannel` + `AppSettings.instrumentalOutputChannel` (`0` = combinado); picker nuevo "Canal de la instrumental" en Ajustes › Hardware junto al de salida, y el mismo selector en el paso Audio del asistente de calibración (2026-09-05, mismo día): "Combinado" es un estado explícito (no se cae al primer par si el dispositivo cambia y el par ya no existe, cae a combinado). **Botón "Reiniciar cortes" (2026-09-05, F.69):** `CalibrationWizardModel.resetFaderCuts()` vuelve el contador del paso Fader a 0 sin tocar cutIn/histéresis — repetir los diez cortes con otro ajuste sin salir del paso. **El plato para en firme con el timecode real; silencio antes del principio (2026-09-05, F.70, ADR-076):** `PracticeSession` separa `timecodeDriving`/`lastTimecodeAt` de `scrubbing`/`lastScrubAt` (F.44) — mientras hay señal real ninguna física sintética toca la velocidad, y al cortarse (>80 ms sin muestra) el plato se para EN FIRME (antes volvía la fricción de F.08, pensada para soltar el trackpad, y "el teal" seguía deslizando solo). El tope de abajo de `coastPlatter` deja de ser `posLo` (principio del sample) y pasa a `posLoFloor = posLo - patternSpan·2.5` (mismo margen que `posHi` hacia delante): ningún scratch real lo alcanza, solo frena el martilleo sintético de `scrollBy` (rueda del ratón), que acumula impulso sin límite. **Botón "Refrescar dispositivos" (2026-09-05, F.71):** paso Audio del asistente — re-enumera CoreAudio a mano sin salir de Calibración; la lista normal solo se refresca al ENTRAR en la pantalla, así que un hipo del driver justo tras un replug de USB mientras ya estabas dentro (visto arreglando F.67 con la Rane 72) se quedaba con la foto vieja hasta salir y volver a entrar. |

## Registro de re-sellados

Cada vez que un modulo SEALED se modifica, una linea aqui con el ADR que lo
justifica. Si esta tabla crece rapido, el diseno de modulos esta mal.

| Fecha | Modulo | ADR | Motivo |
|---|---|---|---|
| 2026-09-01 | XFRender | ADR-038 | Rejilla de negras/compas en la autopista. Aditivo: `HighwayGeometry.beatsPerBar`, `HighwayFrame.beatLines`/`barLines` (con default), pintado en `HighwayScene`/`HighwaySVG`. 25 goldens regenerados. Los 34 tests sellados intactos + 7 nuevos. `apiVersion` sigue 1 |
| 2026-09-01 | XFRender | ADR-040 | La sombra de la autopista se parte donde el fader esta cerrado (mute = hueco, notacion TTM). Aditivo: `HighwayFrame.discSegments` (con default), pintado en `HighwayScene`/`HighwaySVG`. 25 goldens regenerados. Los 41 tests anteriores intactos + 4 nuevos. `apiVersion` sigue 1 |
| 2026-09-01 | XFRender | ADR-041 | Hueco encima del patron: `HighwayGeometry.patternFill` (fraccion de la banda que ocupa el patron desde abajo, default 1). `y(forPosition:)` lo aplica; la traza que se pasa del pico se extrapola hacia arriba. Sin regenerar golden (default = identico). 1 test nuevo. `apiVersion` sigue 1 |
| 2026-09-02 | XFRender | ADR-044 | Phantom clicks: `HighwayFrame.phantomMarks` (cambios de sentido del disco con el fader abierto; el vinilo se para un instante y corta el sonido sin fader, notacion TTM). `HighwayLayout` los calcula por `wrapped` (periodico, anti-deriva), `HighwayScene`/`HighwaySVG` los pintan como tick corto. 25 goldens regenerados. 4 tests nuevos. `apiVersion` sigue 1 |
