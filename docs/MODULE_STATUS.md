# Estado de los modulos

Estados: `TODO` (no existe) · `WIP` (en desarrollo) · `SEALED` (terminado y
congelado, ver `ARCHITECTURE.md` seccion 6) · `BROKEN` (regresion detectada).

> Total de la suite: **605 tests en verde** (`swift test`, 2026-09-03).

| Modulo | Capa | Estado | Sellado el | Tests | Notas |
|---|---|---|---|---|---|
| `CXFAudioCore` | 0 | WIP | — | 56 verdes | `xf_ring` (B4.1) + `xf_player` resampling antialiasing + **bucle** + **puerta por velocidad** (el plato parado no suena ni zumba) (B4.3) + `xf_metronome` (B4.4, ADR-007) + `xf_engine` motor RT + host CoreAudio (B4.2: nucleo `xf_engine_render` testeable; host `xf_engine_start`/`xf_engine_start_output` -solo salida-, sin tests) + **base instrumental como 2o player, tempo-lock por resample** (ADR-039) + getters `xf_engine_scratch_playhead`/`_seek_scratch` + `xf_rt`. El ancla de posicion del scratch mueve por velocidad media de bloque, no a saltos de cabezal (ADR-042: quita el crujido al scratchear). Corregido un SIGILL latente en `xf_engine_stop` (`os_workgroup_leave` desde el hilo equivocado). Falta la puerta de latencia en hardware + Instruments (B4.5) para sellar (B4.6) |
| `XFPrimitives` | 0 | SEALED | 2026-09-01 | 4 verdes | apiVersion 1. `MotionSample` / `FaderSample` (value types compartidos capture↔analysis). ADR-033 |
| `CXFTimecode` | 0 | WIP | — | 7 verdes | xwax 1.10 vendorizado intacto (B5.1) + wrapper `xf_timecoder` en modo relativo (B5.2), hamster/reverse (B5.3), confianza + dropout (B5.4). Tests con señal de cuadratura sintética. Falta pasar un vinilo real para sellar (B5.5) |
| `XFClock` | 1 | SEALED | 2026-08-31 | 34 verdes | apiVersion 1. Tick/PPQ 480, Tempo, TimeSignature, HostClock, ClockMap, Transport. ADR-031 |
| `XFNotation` | 1 | SEALED | 2026-09-01 | 20 verdes | apiVersion 1. Modelo XFN + compose (port de xfn_core.py) + crop con tramo parcial + variantes offset/amplitude/mirror/swing/subdivision + ScoreEvents (== SCORING.md) + golden vs library-v0.1.json. ADR-032 |
| `XFProfiles` | 1 | SEALED | 2026-08-31 | 24 verdes | apiVersion 1. Parser INI propio (ADR-019), extends + herencia circular, validacion == xf_profile.py, autodeteccion con comodines, precedencia bundle/usuario |
| `XFCapture` | 1 | WIP | — | 76 verdes | Protocolos (B6.1), teclado (B6.2), `TimecodeMotionSource` (B6.3), `FaderBinarizer` (B6.5), `.xfsession` + `Replay*Source` (B6.6), HID, `AudioReturnFaderSource` (B6.4b, tono piloto), `MidiFaderSource` + `AudioEnvelopeFaderSource` + conectores CoreMIDI/IOHIDManager (B6.4, los conectores sin tests: necesitan el dispositivo). **`PracticeCommandMidi`** (ADR-054): decodificador puro nota/CC → comandos de práctica; lee `[transport]` del perfil + overrides; `MidiBinding.learned(...)` traduce un mensaje a asignación (MIDI Learn). **`MidiMonitorConnector`** (CoreMIDI genérico, sin tests): monitor de todas las fuentes MIDI que alimenta el "Aprender" de Ajustes. Para sellar (B6.7): smoke test con hardware |
| `XFAnalysis` | 1 | WIP | — | 19 verdes | Emparejado de clicks, DTW de contorno, sigma/sesgo, amplitud, scoring por evento (== SCORING.md), 3 estrellas (ADR-025), diagnostico NL (ADR-018). `MotionResampler.velocity` ya no revienta si el patron dura mas que la toma (underflow `UInt64` -> SIGILL); devuelve el extremo, como `position`. `Diagnoser` **siempre devuelve >=1 frase** (antes una toma limpia de un patron sin clicks -baby- daba lista vacia). B8.5 sintetico completo: `ReplayScoringTests` fija las invariantes good/late/sloppy contra un patron de cada nivel 1-4 (forward-cut / transformer-2 / flare-1c / flare-2c); `GoldenReplayScoringTests` congela el `Report` numerico exacto de las 12 tomas en `Fixtures/golden/analysis/` (redondeo a 4 decimales + tol 1e-9, `make golden-update`). Sin sellar: B8.5 necesita `.xfsession` reales (hardware), B8.4 umbrales por afinar contra ellos |
| `XFPersistence` | 1 | SEALED | 2026-09-01 | 44 verdes | apiVersion 1. `XFDatabase` (puerta unica, GRDB 6.x, migracion `v1` con 10 tablas). Records + metodos por concern: histrico de tomas + eventScores (B10.2/B10.6), progreso agregado (B10.7, == SCORING.md §3), dominado + desbloqueo de variantes (B10.8), repeticion espaciada 1/3/7/21 (B10.3), calibracion por dispositivo (B10.4). Reglas de producto dentro, catalogo fuera. ADR-035 |
| `XFEngine` | 1 | SEALED | 2026-09-01 | 38 verdes | apiVersion 1. Facade `Session` (B9.4, ADR-034) cableando `SessionMachine` (fases), `BPMLadder` (tempo adaptativo: 3 aprobados suben, 2 fallos bajan) y `UnlockRule`/`UnlockTracker` (desbloqueo por N compases buenos seguidos, no por media). Todo struct valor que avanza por eventos como `Transport`. Una serie se aprueba por streak de compases limpios, no por media (ADR-034) |
| `XFDesign` | 2 | SEALED | 2026-09-01 | 7 verdes | apiVersion 1. Tokens (color, espaciado, tipografía, HitLevel) + componentes base (XFCard, XFButtonStyle, BPMStepper, HitBadge). macOS 11. ADR-036 |
| `XFRender` | 2 | SEALED | 2026-09-01 | 50 verdes | apiVersion 1. `HighwayLayout` (autopista: fantasma + capa de usuario teñida por `HitLevel` + cabeza de lectura + **rejilla de negras/compas** + **phantom clicks**, loop con el patron, anti-deriva `frame(T)==frame(T+L)`). `ScopeLayout` (scope circular, Lissajous reconstruido de `position`/`confidence`). `HighwaySVG` (golden SVG de los 25, con rejilla). Escenas/vistas SpriteKit+SwiftUI delgadas, sincronizadas al reloj de AUDIO. **B7.2b cerrado (2026-09-03):** 60 fps estables medidos en el MBP 2015 Intel (overlay `FrameRateMeter` + `ScreenRefresh` en XFApp, sin tocar este módulo). ADR-036, ADR-038, ADR-040, ADR-041, ADR-044 |
| `XFTestKit` | — | TODO | — | — | |
| `XFApp` | 3 | WIP | — | 227 verdes | Ensamblaje: `CatalogLoader` (carga `data/` + `PrimitiveSet`), assemblers (Home/Library/ExerciseDetail/Variant/MyTable), `AttemptRecorder`, `EngineHandle` (envoltorio de `xf_engine`, con `restartOutput` para cambiar el buffer en caliente), `AppModel` (coordinador + navegacion; `scratch(exerciseId:variantId:)` aplica el transform de la variante, ADR-043) y `AppRootView` (sustituye `HomeScaffoldView`, ya borrada). Pinchar un truco abre su **ficha** (dibujo + historia + variantes con marca) antes de practicar; la onda de la instrumental va arriba con la rejilla alineada a la autopista. El `@main` monta `AppModel.boot()` con el loader que toque: `BundleContentLoader` (recursos del `.app`, los copia `make app`) si el bundle trae el catalogo, si no `RepoContentLoader` (repo, para `swift run`/tests). Practica rudimentaria jugable con audio (scratch por velocidad + ancla-trim ADR-042, base instrumental, medidor de clip, volumenes/sensibilidad/buffer por sesion); ajustes (buffer, nombre) persistidos en plist local, los volumenes no (eran la causa de "no suena nada"). + B11 completo (16 tareas): asistente de calibracion (B11.1), Home + matriz + racha (B11.2), practica/autopista (B11.3), resultados + diagnostico (B11.4/B11.13), modo libre 30 s (B11.5), navegador de libreria (B11.6), ajustes (B11.7), accesibilidad (B11.8), Mi mesa (B11.9), mapeo MIDI/HID (B11.10), exportar perfil (B11.11), permiso de microfono (B11.12), progreso por variante (B11.14), selector de variantes (B11.15), Rosetta (B11.16). Modelos/formatters testeables + vistas SwiftUI (macOS 11); el cableado con datos reales y la verificacion visual quedan para cuando corra la app. **+ (sep 2026)**: practica sectorizada (ADR-053), **Freestyle** (nav), grabacion de linea **anclada a la instrumental** (ticks, cuadre a compases, claqueta, `.xfsession`), **"Puntuar la toma"** -> `XFAnalysis` -> resultados, tecla `2` reinicia la base, **F.3** `SampleTrim` (punto cero) + **biblioteca de samples** (`AppSettings.sampleLibrary`, menú en el panel Mezcla) + **cue points A/B** (`PracticeSession.jumpTo(sampleFraction:)`) + aviso de loop rítmico (`TempoAnalyzer`), **F.0** `WarmupPlanner`/`WarmupOxidation`/`WarmupView` + nav "Calentar" (+ **rutina de arranque** fija sin historial: Forward Cut → Reverse Cut → Chirp → Transformer, 8 frases de 2 compases; arranca en "repite conmigo"). El calentamiento entero corre en **una sola sesión**: `startWarmupSession(rows:)` → `[WarmupStep]`, `PracticeSession.reload(scratch:)` cambia el patrón en caliente y `LivePracticeView.advanceWarmup()` encadena al completar las frases (barra "Calentamiento i/N"). El plan es **editable** en `WarmupView`: borrar fila, ×2 / ÷2 sobre el nº de frases, "+" añade de `AppModel.warmupLibrary` (`WarmupPickable`). En "tu turno" del call & response se atenúa **solo el fantasma gris**, no la traza teal (antes se oscurecía todo). **Instrumental subida = bucle de N compases** (`InstrumentalLoop`, puro; ADR-060): suena a velocidad natural y el BPM de la rejilla se **deriva** de N y de la duración, así el metrónomo y el compás quedan clavados al loop pase lo que pase con la detección de tempo (que solo adivina N). Botones **−/+** en el panel Base para corregir los compases. **Metrónomo alineado con la rejilla**: "reiniciar la base" (tecla `2`), `retempo` (×2/÷2), importar línea y cargar otra base ahora hacen `engine.seek(tick: 0)` además de `session.resyncClock()` — el metrónomo va con `e->tick` del motor, no con el reloj de la sesión, y antes se quedaba en su fase vieja. **Call & response**: al empezar tu turno el fader se **reabre** (el fantasma pudo dejarlo cerrado al final de la escucha y arrancabas mudo hasta tocar Espacio). **F.0 cerrado**: "Puntuar (calentamiento)" → `AppModel.scoreWarmupTake` → `settleWarmupTake` (`mode:.warmup`, no cuenta para estrellas, repetición espaciada, `setOxidized` si <2★); el resultado (estrellas · % · aviso de oxidación) se enseña en un panel dentro de la práctica, sin ir a `.results`. **F.4** `TakeVideoExporter` (mp4 con la **proporción de la ventana**, **audio** del `xf_engine` offline **con la mezcla del mixer**, barra de progreso, la línea **refleja los cortes de fader**, **fps/resolución en Ajustes**; ADR-056/057). **MIDI de comandos** (`PracticeCommandMidi` en XFCapture + `AppModel.midiCommands`/`practiceCommandEvents` + fila en Ajustes; ADR-054). **MIDI Learn** (ADR-059): `MidiLearnModel` + `MidiMonitorConnector`; en Ajustes seleccionas el comando, pulsas "Aprender MIDI" y el siguiente control que muevas queda asignado (o lo escribes a mano). **Miniaturas TTM**: la celda dibuja la curva del disco entera, tramo lleno = suena / a rayas = cortado, + recuadro "cómo leer el gráfico" en Home (ADR-055). **Ajustes** reescrito sin `Form` (macOS 11; ADR-058) y partido en dos **pestañas** (General / MIDI). Pantalla de carga con cita (`citas.md`). Splash animado. `SplashView`, `XFWordmark` |

## Registro de re-sellados

Cada vez que un modulo SEALED se modifica, una linea aqui con el ADR que lo
justifica. Si esta tabla crece rapido, el diseno de modulos esta mal.

| Fecha | Modulo | ADR | Motivo |
|---|---|---|---|
| 2026-09-01 | XFRender | ADR-038 | Rejilla de negras/compas en la autopista. Aditivo: `HighwayGeometry.beatsPerBar`, `HighwayFrame.beatLines`/`barLines` (con default), pintado en `HighwayScene`/`HighwaySVG`. 25 goldens regenerados. Los 34 tests sellados intactos + 7 nuevos. `apiVersion` sigue 1 |
| 2026-09-01 | XFRender | ADR-040 | La sombra de la autopista se parte donde el fader esta cerrado (mute = hueco, notacion TTM). Aditivo: `HighwayFrame.discSegments` (con default), pintado en `HighwayScene`/`HighwaySVG`. 25 goldens regenerados. Los 41 tests anteriores intactos + 4 nuevos. `apiVersion` sigue 1 |
| 2026-09-01 | XFRender | ADR-041 | Hueco encima del patron: `HighwayGeometry.patternFill` (fraccion de la banda que ocupa el patron desde abajo, default 1). `y(forPosition:)` lo aplica; la traza que se pasa del pico se extrapola hacia arriba. Sin regenerar golden (default = identico). 1 test nuevo. `apiVersion` sigue 1 |
| 2026-09-02 | XFRender | ADR-044 | Phantom clicks: `HighwayFrame.phantomMarks` (cambios de sentido del disco con el fader abierto; el vinilo se para un instante y corta el sonido sin fader, notacion TTM). `HighwayLayout` los calcula por `wrapped` (periodico, anti-deriva), `HighwayScene`/`HighwaySVG` los pintan como tick corto. 25 goldens regenerados. 4 tests nuevos. `apiVersion` sigue 1 |
